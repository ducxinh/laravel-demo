# Laravel Demo

### Seeding the Database

To seed the database with test data, run the following commands:

```bash
php artisan db:seed --class=TestUserSeeder
php artisan db:seed --class=TestProductSeeder
```
